'use strict';

const taskManagementTypes = require('..');
const assert = require('assert').strict;

assert.strictEqual(taskManagementTypes(), 'Hello from taskManagementTypes');
console.info('taskManagementTypes tests passed');
